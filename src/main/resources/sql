CREATE DATABASE kayakbooking_database;

CREATE USER Viggo WITH PASSWORD 'Viggo123Password';
GRANT ALL PRIVILEGES ON DATABASE invoiceMailer_database TO Viggo;

DROP TABLE IF EXISTS orders;
DROP TABLE IF EXISTS failed_processed_policy;
CREATE TABLE orders (
    order_id VARCHAR(255) NOT NULL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    phone VARCHAR(15),
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    is_email_send BOOLEAN NOT NULL DEFAULT FALSE,
    kayak_one INT NOT NULL DEFAULT 0,
    kayak_two INT NOT NULL DEFAULT 0,
    kayak_one_two INT NOT NULL DEFAULT 0
);


CREATE TABLE failed_processed_policy (
    id SERIAL PRIMARY KEY,
    order_id VARCHAR(255) NOT NULL,
    name VARCHAR(255) NOT NULL,
    date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    retry_count INTEGER DEFAULT 0,
    message VARCHAR(512) NOT NULL
);


INSERT INTO orders (
    order_id,
    name,
    email,
    phone,
    order_date,
    is_email_send,
    kayak_one,
    kayak_two,
    kayak_one_two
)
VALUES (
    'ORDER/000000003/2024',
    'Jan Kowalski',
    'jan.kowalski@email.com',
    '123456789',
    '2025-03-09 14:30:00',
    true,
    2,
    1,
    3
);


INSERT INTO failed_processed_policy (order_id, name, retry_count, message)
VALUES
  ('FV/000000009/2024', 'EmailPolicy', 1, 'Failed to send email due to connection timeout');

SELECT * FROM orders;
SELECT * FROM failed_processed_policy;

 psql -U postgres -d kayakbooking_database

Viggo123Password

http://localhost:8080/get-kayakBookings-data?invoiceId=ORDER/000000001/2024
http://localhost:8080/get-kayakBookings-data?addressEmail=Viggo@wp.pl
http://localhost:8080/get-all-unique-email

    UPDATE kayakBookings  SET is_email_send = false WHERE invoice_id = 'FV/000000011/2024';
      UPDATE failed_processed_policy  SET retry_count  = 2 WHERE invoice_id = 'FV/000000011/2024';